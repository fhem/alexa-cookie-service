services:
  alexa-cookie-service:
    build: .
    container_name: alexa-cookie-service
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "18080:8080"
      - "18090:18090"
    volumes:
      - ./data:/data
